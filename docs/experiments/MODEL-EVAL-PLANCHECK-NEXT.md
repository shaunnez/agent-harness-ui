# PlanCheck model evaluation — next run

Prepared 23 September 2026 for continuation after Shaun compacts this task. This is a bounded execution handoff, not a new model ranking or an instruction to change production defaults. Start with case qualification, then run the controlled comparison below if the admission gates pass. Do not ask Shaun to choose a task again.

## Outcome and cases

Establish whether the current Harness model policy delivers an independently accepted PlanCheck UI/backend/PostgreSQL change, and whether changing Implement/Repair from GPT-6 Sol High to Claude Sonnet 5 High improves acceptance enough to justify its extra usage. Hold every other stage policy, workflow setting and allowance fixed. Record first-pass and eventual acceptance separately.

1. **First, qualify a medium historical replay:** PlanCheck [PR #317](https://github.com/EversorAI/eversor-plancheck/pull/317), “Persist construction match candidates.” Pre-fix base `21b63f5c6f3d686e6142cc82276a565cd425909a`; reviewed reference `17be14a74f7a0669d6a69cbf6f02f68f69763318`. This is a smaller first vertical case than the older email feature: it adds a dbmate migration, persists and projects candidate scenarios, and lets a reviewer select a candidate in the UI without silently counting an unresolved amount. Its reference changed 14 files, roughly 300 added lines. Freeze a user-facing brief from the reviewed behavior; do not reveal the reference commit, patch or hidden checks to trial agents.
2. **Then qualify, but do not automatically dispatch, a hard confirmation case:** PlanCheck [PR #143](https://github.com/EversorAI/eversor-plancheck/pull/143), Request-page email delivery, pre-fix base `d01cc4d1`, reference `f15c3853`. It spans UI, backend, a migration and failed-send reporting. Use a fake mail transport and disposable database; no real email. Its larger change makes it a poor first runner smoke test. Decide whether to dispatch it only after reviewing the medium-case results and provider allowance.
3. Keep the current [recovered REMEDY retry trace](/Users/shaun/projects/eversor-plancheck/docs/tickets/TICKET-a-successful-retry-leaves-no-trace.md) and [explicit catalogue override](/Users/shaun/projects/eversor-plancheck/docs/tickets/TICKET-no-path-to-override-account-catalogue-import.md) as real delivery candidates. They are not golden cases until their live code state, acceptance checks and reviewed reference are qualified. Ticket status alone is unreliable: the “drafting unresolved outcome” ticket still says unassigned although its key behavior is already implemented.

## Admission before any provider call

- Refresh Harness and PlanCheck refs, inspect both working trees, existing worktrees, active tasks, provider availability, services and durable evaluation receipts. Do not duplicate a running or completed trial. PlanCheck was clean at `3ba24541` after the local preflight; Harness main was updated to `d73d790` before this handoff. These are observations, not instructions to reset later work.
- Create isolated historical checkouts and disposable PostgreSQL databases. Keep the registered PlanCheck checkout, its local database, user-run services and private customer artifacts out of the trials. Never edit Harness SQLite directly to influence a task.
- At the frozen pre-fix base, run the relevant repo quality/test commands and identify unrelated historical baseline failures. At the reviewed reference, the same baseline must pass. If necessary, version a minimal common baseline repair applied to **both** sides; otherwise reject this case. Do not classify an old formatting/type failure as a model miss.
- Build an independent checker that fails the unchanged base, passes the reviewed reference, and rejects at least one plausible defective candidate. Assert durable candidate IDs, unresolved/ambiguous and incompatible states, no amount entering totals before a reviewer selects a scenario, correct saved selection/revision, and UI behavior through a browser. Use isolated PostgreSQL integration checks. Keep the checker and reference outside the agent's accessible task checkout. Snapshot the brief, base, dependency versions, checker and policy before dispatch.
- The current PlanCheck checkout passed `make lint`, `make backend-quality`, `make backend-compile`, `make frontend-build`, `make test` (5,606 backend passed; 556 database-gated skips), and isolated `make integration-test` (548 passed, no skips). The one-process local E2E run was **not** green: 151 passed, 3 skipped, 16 Processing screenshot failures from stale ignored Mac baselines plus shared suite state. The 16 screenshot checks passed alone on a fresh database after refreshing only the ignored Mac set; CI at the current SHA passed isolated Linux Playwright shards. For grading, use fresh, per-trial databases and behavioral browser assertions; do not score historical Mac snapshots as product defects. See the [readiness record](/Users/shaun/.codex/model-evaluation/20260923/plancheck-readiness-v1/READINESS.md).

## Controlled comparison

- Run **two trials per arm** on the qualified medium case: A = current Codex-supporting-role policy with Sol High Implement/Repair; B = the same supporting roles with Sonnet 5 High Implement/Repair. Pin both roles so Repair escalation cannot silently change the arm. Keep the existing generous task/call allowances and retry caps identical; do not introduce a new low timeout or token ceiling. Verify the actual dispatched provider/model/effort in each receipt. Interleave arm order and use at most two simultaneous isolated trials if local resources and provider allowance permit.
- Grade each exact final candidate outside Harness using the frozen checker. Record every failed/invalid attempt and repair, first-pass and eventual acceptance, elapsed time, input/output/cached tokens, and **API-rate estimates** separately from actual subscription charges. Request one independent blind review per behavior-passing candidate; adjudicate findings against the frozen contract. Internal green Dev Review/Test/Final Review is not independent acceptance.
- If case admission fails, provider allowance is unavailable, a trial diverges from its policy, or a grader defect appears, stop new dispatch. Preserve receipts; fix/version the apparatus and rerun admission before a new comparison. Do not replace or erase a failed arm. Do not dispatch the hard case merely to accumulate runs.
- Output a short result table, candidate-linked evidence, and a recommendation to keep or change the provisional policy. This run ends at graded, PR-ready artifacts. No batch of benchmark PRs, merge, deployment, production default change or real customer/provider workflow follows automatically.

## Git transport and PR delivery

The registered PlanCheck checkout is `/Users/shaun/projects/eversor-plancheck`. Both its `origin` and tracked `evesor-plancheck` remotes were changed **locally** from HTTPS to `git@github.com:EversorAI/eversor-plancheck.git`. After that, ordinary `git pull --ff-only` worked and `git push --dry-run origin HEAD:refs/heads/codex/plancheck-git-preflight` confirmed SSH push access without creating a branch. Preserve this repository-local SSH transport; do not switch GitHub accounts globally.

Git access does not solve Harness's `gh pr create` identity. In this shell the default `gh` account cannot read the private EversorAI repo; the configured `shaunnesbitteversor` account can when its token is supplied only to that command/process. Before any **real** PlanCheck “Approve & raise PR,” verify the running Harness process has the Eversor `gh` identity and test the exact delivery path without exposing a token. Benchmark trials stop before PR publication, so this is a separate activation gate, not a reason to relabel a graded code candidate as failed.

## Continuation

After compaction, read this file and the readiness record, refresh live repository/task state, then begin the no-provider medium-case qualification. Update this handoff with the frozen case/checker paths, trial IDs, receipts and results as work proceeds. Do not start a new broader campaign or revisit previously graded Harness H02/H05/H06 trials for this PlanCheck comparison.
