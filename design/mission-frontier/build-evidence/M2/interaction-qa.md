# M2 interaction checks — 6 September 2026

Mode: in-memory fixtures at port5199, 1568×1003, CUA IAB tab6. Actual CLI journey is separately documented in M3.

- Canvas worker pick selected PC-153 independently of project art; terrain drag and zoom preserved identity.
- Selected-label camera transform remained identical after opening/closing Grill and visiting retained scouts evidence.
- Custom answer draft survived evidence/back, closing/reopening, simulated disconnect and reconnect. Offline submission was disabled with an explicit notice. Reconnected submission recorded the answer; specification remained an explicit separate action.
- Found and fixed shared fixture Grill-session references: each fixture is now deeply independent. Browser rerun answered PC-153 while MS-086 remained at 0/1 unanswered, with its own notice-delivery question (`question-isolation.png`). Regression test covers the retained unanswered sibling.
- All12 fixture task IDs were opened through the journal and returned using Back; heading identity matched every ID (`journal-selection.json`). Search PC-153 selected one row. Cleared search + History returned only completed PC-131. Needs you returned6 rows, excluding running/dependency, queued and external-delivery tasks.
- PC-148 repair queue opened exact Dev-review reason, retained findings, and same-task Implement navigation. Future Test, Final review and Approval remained disabled. Watch agent opened retained completed run R-PC-148-dev-review-1, with frozen6m duration and parked worker (`repair-agent.png`).
- AH-054 inspector retained S1 Running next to S2 Failed with the recorded assertion reason. Its HQ label now says `S1 running · S2 failed`; execution failure overlays are labelled `Execution needs attention` rather than review findings.
- Empty/queued task sites do not invent a worker when neither a retained run count nor active run identity exists. Strong work motion now requires an active run ID; scheduling state alone is insufficient.
- Focus now moves into each replacement overlay's header. Closing restores its world origin through the native dialog boundary. Minimap letterboxing correction is covered by a pure test and a browser pan/selection exercise.

Final qualification passed. All25 A0/A1 IDs are integrated. Capsule picking opens the exact retained artifact; final HQ/agent screenshots and matching reference comparisons are in ../M3. Compact desktop and phone overlay checks passed. The100-task camera run,1,000-task search/attention test,20 navigation loops and motion lifecycle checks are documented in ../M3/performance.md. See ../M3/acceptance.md for the complete gate and explicit P3/testing limitations.

Final849×779 check in replacement IAB tab8 exposed a cramped selected-task panel and overflowing minimap buttons. The intermediate breakpoint now reserves more panel width and places camera controls in two rows. After Fit world, all three bases, task identity/actions and all four camera buttons remain visible (`world-849.jpg`). The native viewport was restored after capture.
