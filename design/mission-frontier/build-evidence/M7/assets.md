# A2 integrated asset gate

Qualified on 6 September 2026. One explicitly requested GPT-6 Astra agent produced the A2 station, shuttle and observatory-roof assets in assigned staging directories. The builder retained ownership of code, manifests and integration.

All 35 runtime asset entries and exported motion parts have matching file hashes and dimensions (`asset-verification.json`). Original generated sources, corrected exports, geometry, sockets, alpha checks, calibration images and agent notes remain in `../../assets/staging/`. Runtime files are independent PNG objects in `public/frontier/assets`; no whole-screen image is used as the UI.

The integrated station tour covers triage, scouts, Grill, specification, plan, implement, development review, test, final review, approval and optional design work. Actual browser captures are `station-*.jpg`, summarised in `station-contact.jpg`; `all-stations-hq.jpg` verifies the complete headquarters composition. These were reviewed with the Astra integration contact sheets under `../../assets/staging/A2/mf.base.observatory.roof/qa/integration-review/` and the accepted design references.

The bench, workers and movable tool parts meet their measured sockets. Stage tools remain distinct, worker selection is visible, and important control labels are not covered by art. The launchpad was moved within the detail floor; its shuttle remains independently docked. The observatory roof uses the same base registration. Approval has no running worker when no run exists.

Ten A2 entries now carry `M7-integrated-visual-qualified`. M1/A1 calibration approvals remain preserved. The modular standard architecture, water repetition and slightly different worker/roof proportions remain accepted P3 visual differences from the cinematic study; no placement, upgrades or unlock system was added.
