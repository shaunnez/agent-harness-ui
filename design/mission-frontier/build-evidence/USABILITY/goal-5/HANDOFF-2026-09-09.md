# Goal 5 handoff — implementation paused before acceptance


9 September 2026. Shaun requested a handoff while Goal 5 was running. The goal is paused; U3–U5 and UB1 are implemented but **not accepted as complete**. Application changes are uncommitted. No Goal 5 commit, push or PR has been made. Goal 6 has not started.

## Authoritative source

- Worktree: `/Users/shaun/.codex/worktrees/7237/mission-frontier-goal-5`
- Branch: `codex/mission-frontier-goal-5`
- HEAD/base: `196ec7d369670592dab059f2e8610e8465742c90`
- Remote: `origin`, `https://github.com/shaunnez/agent-harness-ui.git`
- PR #73 is merged. Its merge tree matches the qualified Goal 4 head `bdbd404603b62082b30b12418eef06e1299024d4`; all 97 Goal 4 manifest files matched before these edits. See [baseline.json](baseline.json).
- [source-manifest.json](source-manifest.json) binds the changed implementation and tests at this pause. It is a changed-file snapshot, not a full-tree acceptance manifest.

The original dirty checkout at `/Users/shaun/.codex/worktrees/7237/agent-harness-ui`, the Goal 4 checkout, main checkout, user databases and user services were preserved. Continue here; do not recreate the work, overwrite the original checkout, or push into merged PR #73. No model/provider runs were launched for this goal.

## What is implemented

| Area | Behavior and important source |
| --- | --- |
| UB1 retained history | SQLite schema 3 adds a transactional, typed read projection capped at 5,000 material observations. Observation sequence, opaque source identity, captured upper bounds, bounded pages and explicit retention coverage support catch-up without reading every task history. Existing records establish a baseline; there is no guessed backfill. See `server/workspace-history*.mjs`, `server/sqlite-{store,storage}.mjs` and `src/domain/workspace-history.ts`. |
| Read API | Additive workspace head/history reads and exact task/run reads in `server/retained-evidence-routes.mjs`. Minimal pin metadata excludes transcripts, prompts and repository paths. JSON rollback export remains compatible; the legacy JSON backend explicitly reports catch-up unavailable. |
| U3 decision sessions | Needs you shows project, task, stage, reason, next actor and recorded wait age. Failure/repair-first ordering, stable Previous/Next, a new-items indicator and retained drafts use the existing action eligibility and confirmations. See `runtime/decision-session.ts`, `views/DecisionNavigation.tsx` and app navigation/context. Navigation does not submit a decision. |
| U4 return briefing | Deterministic project/task grouping, completed/repair/blocked history, still-open decisions, exact retained evidence links and completed-run usage. Closing preserves unread state; Mark reviewed acknowledges only the fully loaded captured upper bound. Retention gaps require explicit acknowledgement. Late or undated completions do not become invented interval spend. See `runtime/briefing.ts`, `views/ReturnBriefing.tsx` and `app/use-briefing.ts`. |
| U5 watch pins | Up to four source-scoped task or exact-run pins, persisted locally and synchronized between tabs. Run pins remain bound to the recorded run and become historical. Missing/offline states are explicit. World/HQ strip has one compact row with collapse/overflow. See `views/WatchPins.tsx` and `app/command-memory.ts`. |
| Shared refresh | Workspace head joins the existing refresh cycle; pin reads have concurrency two and reuse task version markers. No independent per-pin poller or full-history hydration. Exact selected runs are revalidated after core refresh, including disappearance. See `runtime/coordinator.ts` and gateways. |

Source replacement clears old pins, drafts, cached reads and pending source-bound results. Browser memory uses `mission-frontier.command-memory.v1` and Web Locks to serialize registry updates. The synchronous fallback in browsers without Web Locks has not been qualified for simultaneous multi-tab writes.

The accepted world/art remains intact. Small layout corrections wrap queue context, keep lighting controls beside the new dock, and isolate the catch-up CSS from the existing New Task briefing classes. No asset agent was needed.

## Verified checks and current failure

| Check | Result at pause |
| --- | --- |
| Relevant Frontier, Frontier API, SQLite/store, orchestration, API characterization and task projection tests | **225 passed, zero failed/skipped** on Node 24.19.0. See `qualified-tests.log` (not retained by the later cleanup snapshot). The narrower history/command/refresh check also passed 20/20 before this run. |
| Final typecheck | Passed. The sequential quality command advanced to lint. |
| Final lint | **Failed:** `scripts/frontier/measure-command-refresh.mjs:31`, Biome `lint/suspicious/noAssignInExpressions`, on `const count = (reads[method] ??= { requests: 0, jsonBytes: 0 });`. Split initialization and lookup into separate statements when work resumes. No implementation fix was made after the pause request. |
| Final formatting / Frontier build / root build / Sites tests | **Not reached** because lint stopped the `&&` chain. Earlier versions passed these checks, but that is not qualification of the final source. Final build log files were never created. |
| Request-volume measurement | Passed its assertions with zero/four pins at 100 and 1,000 tasks. See [request-volume.json](request-volume.json). |
| Complete root suite | Not run for Goal 5. The 225-test selection is not the complete repository suite. |

In each 10.2-second measurement window, four pins added four minimal run reads / 1,324 serialized JSON bytes, with maximum concurrency two. No selected core, activity or run-list reads occurred. Normal and stress runs retained the same polling counts. These are same-machine fixture gateway counts, not measured HTTP traffic or browser frame timings. Hardware: Apple M5, macOS arm64. The existing bundle-size warning remains an earlier known limit until the final builds are rerun.

## Browser evidence and acceptance still open

The following checks were performed in Computer Use during implementation. Screenshots support the visible states; they do not replace the unfinished journeys or qualify every subsequent source edit.

| Area | Observed evidence | Still required |
| --- | --- | --- |
| Laptop space | At 1280 × 720, maximized task content was **456 px** and normal Watch activity **242 px**, above the 420/240 px targets. [Task](task-max-laptop.png), [Watch](watch-normal-laptop.png). | Recheck affected destinations on final source, scrollbar manipulation and long content. |
| Draft navigation | Next then Previous retained an unsubmitted custom Grill answer; recorded answer count stayed zero. Keyboard Tab reached the answer action. [Draft](decision-draft-laptop.png). | Complete the cross-project answer → repair → candidate approval journey; failed-action retention; another client resolving/deleting the selected item; candidate replacement while confirmation is open; full keyboard/Back/camera/filter restoration. |
| Briefing | Completion → completed run → repair → renewed blocker all appeared. Close/reopen retained the captured bound; later arrivals remained unread. A retention gap disabled acknowledgement until explicitly accepted. [API fixture briefing](briefing-api-laptop.png), [gap](retention-gap-laptop.png). | Final-source browser pass after restarting the owned API; interrupted pagination/reconnect and all missing/renamed/deleted-record interactions. Pure contract tests cover several of these boundaries already. |
| Pins / source isolation | Four mixed task/current-run/historical-run pins fit one row; offline pins said Last known. Pin changes synchronized between two API-backed tabs; pins survived reload. Source replacement cleared pins and an unsubmitted draft. [Offline pins](four-pins-offline-laptop.png). | Complete HQ return paths, keyboard/reduced-motion and missing/archived recovery on final source. |
| Multi-tab checkpoints | A tab with an older captured boundary did not acknowledge later arrivals. A later acknowledgement propagated to the other tab without checkpoint regression. API fixture only; no paid execution. | Final-source regression walkthrough; assess the non-Web-Locks fallback explicitly if supported. |
| Desktop | 1440 × 900 and 1488 × 1058 dimensions and no document overflow checked. [1440](world-1440.png), [1488](world-1488.png). | Final matched normal/stress browser frame measurements and request/payload comparison at a documented viewport. |
| Narrow / native zoom | Native Chrome 200% was reached and recorded in [native-zoom-setting.txt](native-zoom-setting.txt). | **The 200% and narrow-width flow walkthroughs were not completed.** Merely reaching 200% is not a pass. |

`briefing-laptop.png` predates the catch-up CSS collision fix. Use `briefing-api-laptop.png` as the later layout reference. Existing screenshots are dated evidence, not a claim that the entire final source was visually accepted.

During handoff cleanup, Chrome was restored to native **100%** and verified (1920 × 873 CSS viewport, DPR 1). The temporary Chrome tab was closed. The in-app viewport override was reset and a fresh fixture preview was left open for continuation.

## Running preview and owned fixture

- [Sample preview](http://127.0.0.1:5205/?mode=fixture&art=cinematic#world) is open. Frontend PID at handoff: `42893`, exec session `20262`; Vite serves this worktree with HMR.
- API fixture listens on `127.0.0.1:4325`, PID `42837`, exec session `77547`.
- Frontend was started with `AGENT_HARNESS_API=http://127.0.0.1:4325 npm run dev:frontier -- --port 5205`.
- API fixture root: `/var/folders/nr/bpphtrj50gz4_rjqtsdm36_00000gp/T/mission-frontier-fixture-gyS96T`. Its `frontier-isolated.json` identifies provider `fixture`, `repository/`, and `data/tasks.sqlite3`.
- The API is plain Node, **not hot-reloading**. It predates the final backend fixes. Before further API-backed visual acceptance, verify the marker/process ownership, then restart **only this owned API** with the same root. Do not stop the user's APIs or use their database.
- Restart entry point: `FRONTIER_QA_PORT=4325 node scripts/frontier/qa-server.mjs --root <the existing fixture root>`. Use the supported Node runtime below. Do not pass `--codex`.
- Disposable API tasks AH-001–AH-004 were seeded for review. AH-001 accumulated completion/repair/block history; it is not a real delivery. A fixture control process suppressed startup recovery locally while making those observations; production recovery code was not disabled.

PIDs/session IDs are handoff observations and must be revalidated. Existing user services on 5199 and the Goal 4 services on 5204/4324 were left alone. No acceptance/check process remains running from this goal; both development servers remain available.

## Journal and delivery status

The separate journal repository is unchanged and clean at `/Users/shaun/.codex/worktrees/7237/agent-harness-ui/journal-site`, commit `b179997def57be78138560eb83466faa5ff38714`. Its local publication receipt records version 10 and [The zoom check is closed](https://mission-frontier-journal.shaunnesbittuk.chatgpt.site/notes/the-zoom-check-is-closed/). That published checkpoint still describes the pre-merge Goal 4 status; **Goal 5 has not been added or published**. The receipt's custom audience was not revalidated remotely during this handoff.

After acceptance, update the journal from the isolated qualified source and preserve its existing audience, original design study and dated entries. Follow its own `AGENTS.md` and Sites workflow. Do not copy application changes over the original dirty checkout or replace the game's hosting identity with the journal's identity. Do not publish this internal handoff verbatim: it contains local process and filesystem details.

## Next steps, in order

1. Verify this worktree, branch, diff, manifest and owned service identities. Preserve all current files; this is a continuation, not a new implementation.
2. Fix the single measurement-script lint error. Run typing, lint, formatting, both builds and Sites tests. Repeat affected tests if code changes; retain new output and bind it to the final source.
3. Restart only the owned API fixture to load the final backend. Finish the open U3–U5 browser rows above, especially stale candidate confirmation, keyboard/Back restoration, native 200%/narrow reflow, and final pin/briefing regression checks.
4. Finish matched viewport request/payload and normal/stress performance evidence. Do not label gateway serialization as measured network traffic or carry forward old FPS as a fresh measurement.
5. Review the final diff, write the final acceptance matrix/manifest, and update the separate journal for its existing audience. Prepare a new Goal 5 PR when the current checks and acceptance pass; PR #73 is already merged. No commit/push/publication was performed for this pause.

Goal 6 remains separate: UB2 incremental activity, meaningful recorded world feedback and combined qualification. The real implementation → repair → approval → PR journey, native background/motion lifecycle and existing bundle warning are separate operational follow-ups; do not use the user's tasks as test fixtures.

Supported runtime directory: `/Users/shaun/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin` (Node 24.19.0 used here). The 225-test selection was:

```sh
node --test tests/frontier/*.test.mjs tests/frontier/api/*.test.mjs tests/sqlite-store.test.mjs tests/store.test.mjs tests/orchestrator.test.mjs tests/api-contract-characterization.test.mjs tests/task-projections.test.mjs
```

Suggested continuation prompt:

```text
Resume the paused Goal 5 in /Users/shaun/.codex/worktrees/7237/mission-frontier-goal-5.
Read design/mission-frontier/build-evidence/USABILITY/goal-5/HANDOFF.md first.
Preserve the uncommitted implementation and other checkouts/services. Fix the
recorded lint error, load the final backend in the owned fixture, and complete
the remaining U3–U5/UB1 acceptance checks. Keep evidence bound to the final source
and update the separate journal for its existing audience. Do not start Goal 6,
run paid workflows, mutate unrelated live tasks, merge a PR or publish the game.
```
