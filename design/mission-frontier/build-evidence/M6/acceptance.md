# M6 — agents, policies, usage and connection

Passed for the local v1 build on 6 September 2026, with browser-host verification limits recorded below.

- Agent roster separates active run instances, the canonical role catalogue and history. Historical workers retain their recorded model/effort and stopped state (`agents-final.jpg`). Scoped run loading has bounded concurrency and paging; a refresh preserves requested history without hiding a gap.
- Skills expose read-only local instructions, capability scope and supported policy controls. The six scout specialties share their coordinator policy; no invented editable skill source, skill installer, success rate or permanent employee system is presented.
- Execution settings cover model allowlisting, fallback, all three workflow profiles, per-role defaults, Grill automation and independent Codex/Claude design choices. Saving Implement as Sol High left the existing historical Luna XHigh run unchanged (`skills-policy-saved.jpg`, `history-after-default-save.jpg`). Snapshot immutability and invalid-policy rejection passed against the real API boundary.
- World camera, follow, labels, label size, motion and audio preferences persist on this device. Follow/motion/label changes survived reload; defaults were restored to motion on, normal labels, follow off and audio off. Motion off stops only the renderer (`../M7/motion-off.json`).
- Usage project/task/model/date filters operate on retrieved records. The PC-142 sample totals reconciled to 124K input, 62K cached and 20K output, with 50% cache rate. A Sol filter selected its one recorded run; empty scope kept missing values explicit. Task wall time and aggregate run time are different metrics. Costs require an identified rate card and are labelled API-rate estimates; attributable ChatGPT-plan charges remain unavailable.
- The real completed AH-001 investigation was re-opened after the isolated backend restart, with three retained runs, 149K total tokens and its recorded context manifest. Live usage, connection and task-scoped worktree inventory were inspected (`../M7/live-*.jpg`). No live task was dispatched or worktree removed.
- An interrupted connection preserved the draft title, description, repository and policy choices; controls were disabled while offline and restored on reconnect without handoff replay (`../M7/offline-draft.jpg`, `../M7/reconnect.json`). Loading, no records, absent telemetry and partial histories have explicit states.

The usage export button prepares JSON with scope and completeness metadata. Its browser download action was invoked, but this IAB did not emit a download-completion event; OS file receipt is not claimed as verified. Similarly, this host reports automated tabs as visible, so actual OS backgrounding remains a host-specific manual check. The visibility/reduced-motion lifecycle is covered by executable tests and the local motion switch was verified in-browser.
