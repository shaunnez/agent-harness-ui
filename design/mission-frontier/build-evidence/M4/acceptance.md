# M4 — management checkpoint

The bounded B1/B2 implementation and desktop management journey passed. M7 still owns the complete application, responsive and production-build qualification.

- Targeted automated suite: 54 passed, 0 failed, 0 skipped (`management-suite.log`); TypeScript passed after the last M4 logic correction.
- JSON and SQLite stores: policy snapshot/validation atomicity, project rename/archive/restore and archive/create races covered in real API tests. Prior store regression: 25 passed (`store-regression.log`).
- IAB live journey against isolated API4321 created AH-002 without dispatch, attached a 200-byte HTML file, changed Grill to Sol High, reviewed/saved, reloaded, then changed future Implement to Sol High. Both overrides persisted across all three profile matrices. Triage was locked after reaching it. Closed with a retained note; zero runs, original attachment metadata intact. Exact reads retained in live-task-created.json / live-task-closed.json.
- The original live CLI task AH-001 remains completed with its evidence. It was not rerun. The database was backed up before loading new server modules.
- Live project repository validation displayed captured Git head and missing verification manifest. No paid setup proposal was dispatched. Archived the now-inactive project and restored the same ID/path, retaining both tasks. Sample mode separately exercised register, rename, setup proposal/approval and blocked archive.
- IAB sample navigation exercised search/state/project filters, full creation review, future-policy confirmation, closure and nested Escape return with retained query. Attachment and brief survived Add project / Escape before live creation.
- Matched 1568x1003 comparisons: new-task v3, task-policies v2, Projects v2 and journal v3. Corrected compact wizard placement/scroll, base thumbnail, journal selected-panel clipping and missing-time wording. Mobile task state remains visible; full responsive regression belongs to M7.

No real PR was opened, no repository implementation was started, and the user's runtime was not modified.
