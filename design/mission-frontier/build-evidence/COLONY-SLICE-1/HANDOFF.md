# Colony slice 1 — handoff (stopped 16 September 2026)

Shaun stopped the build shortly after kickoff. This is the state to resume from. Read this file, then `design/mission-frontier/colony/README.md`, then the frozen contract.

## Authority

- Frozen contract: `design/mission-frontier/assets/staging/colony-hq-v1/contract.json` (version 1.0.0-frozen). Never edit it. It is generated by `design/mission-frontier/colony/source/generate_colony_pack.py`; if a genuine defect is found, change the generator, regenerate, bump the version, and record why in `JOURNAL.md`.
- Design pack: `design/mission-frontier/colony/` (COLONY-PLAN, HQ-FLOOR-PLAN, INPUT-CONTRACT with per-role checklists, DELIVERY-SLICE with scope and the ten acceptance criteria, TERRAIN-HOLES diagnosis).
- Decisions already taken by Shaun (do not re-ask): overflow lane with "+N" labels approved; hub parcel shown from day one with an empty pad; cutaway camera 40° for HQ only; colony slot persistence browser-local with the appearance record, backend later.

## Branches and worktrees

| Role | Path | Branch | Head | State |
| --- | --- | --- | --- | --- |
| Lead / design | `/Users/shaun/projects/agent-harness-ui/.claude/worktrees/mission-frontier-colony-design-c7dcc1` | `claude/mission-frontier-colony-design-c7dcc1` | `c0d17e2` + this handoff commit | design pack, contract, journal, producer scaffold; main merged up to `e6f783c` |
| Runtime builder | `/Users/shaun/projects/agent-harness-ui/.claude/worktrees/agent-aa898a862c65647f8` | `worktree-agent-aa898a862c65647f8` | `4fa0885` | two commits on top of `c0d17e2`, one uncommitted file; 111 Frontier tests pass, typecheck clean |
| main | `/Users/shaun/projects/agent-harness-ui` | `main` | `6773f41` | two more world-3d commits since the last merge (see below) |

Nothing is pushed. Ports 5199, 5207, 5218, 5219 belong to other people's Frontier servers; use 5241 or higher.

## What exists

### Runtime (branch `worktree-agent-aa898a862c65647f8`)

1. `f30a89c` — `src/frontier/world-3d/colony.ts` (346 lines): contract loader, slot table H/P1–P18, fill rule (lowest free slot at first sighting, createdAt then id, archived keep slot), bridge list per occupied set, browser-local slot persistence added to `appearance.ts` / `useBaseAppearance.ts`. Test tweak in `tests/frontier/3d-proof.test.mjs`.
2. `4fa0885` — `layout.ts` now places bases on colony slots instead of the staggered archipelago; `ProofCamera.tsx` extents and minimap scoping adjusted; `ProofLabels.tsx` touched.
3. Uncommitted: `src/frontier/world-3d/rooms.ts` (206 lines) — stage-to-room mapping and socket allocation, unreviewed and untested. Commit it first or read it and decide.
4. Not started: grey-box colony scene, cameras per contract, manifest v3 in `model.ts`, `integrate-3d-proof.mjs` extension, 10-project stress fixture, 14-task project fixture, new tests, browser captures.

### Asset producer (lead branch, `assets/staging/colony-hq-v1/producer/`)

- `source/hq_lib.py` (423 lines) and `source/export_hq.py` (89 lines): Blender helpers scaffolded from `astra-kit/source/build_kit.py`. No `.blend`, no GLB, no validation, no previews. Treat as a starting point, not as delivered work.

### Terrain builder (lead branch, `assets/staging/colony-hq-v1/terrain/`)

- Empty. The agent was still reading the kit when stopped.

## Merge rule before touching `src/frontier/world-3d/`

Main moved after the last merge. Merge `main` (`6773f41`) into whichever branch you work on before editing world-3d, and confirm a clean tree and green tests first. The new commits touch files the runtime work also touches:

- `1c7abde` shadow throttle: `ProofScene.tsx`, new `shadow-cadence.ts`, test.
- `6773f41` night lamp pool: `ProofBase.tsx`, `ProofScene.tsx`, new `lamp-pool.ts`, test.

Build on top of these, not around them. The runtime branch's `layout.ts` / `ProofCamera.tsx` changes must keep the minimap scoping and focus zoom clamp from `1bc6935` working.

## Next steps

### Runtime builder (resume in the runtime worktree)

1. `git merge --no-edit main`; run `npm run -s test:frontier` and `npm run -s typecheck`. Fix any conflict in `ProofScene.tsx` / `ProofBase.tsx` in favour of main's lamp pool and shadow cadence.
2. Review and commit `rooms.ts` against `contract.hq.stageToRoom`, room sockets, `hubOverflowSockets`, `courtSockets`, then the overflow lane (1.6 m pitch along the room's inner partition, 0.8 m off the wall, "+N" label). Minimum spacing 3.3 m by allocation; never drop a task.
3. Grey-box colony scene at contract coordinates (hex shell, hub, six rooms, bay, parcel disc r 34, ring road, spurs, pads, 27 m bridge boxes) used whenever the manifest lacks colony assets. Cutaway hides `MF_Roof` + `MF_ShellCutaway`.
4. Cameras from `contract.cameras` (exterior/world 28.4°, cutaway 40° offset (32.2, 45, 42.9) span 40, watch = cutaway then follow). Labels for six rooms and "+N", clearing the Needs-you panel and dock at 1568×1003 and 1280×720.
5. Manifest v3 in `model.ts` (v2 still parses), `integrate-3d-proof.mjs` extension that copies the contract and any GLBs found under `colony-hq-v1/{producer,terrain}/` with hash validation, `?scenario=colony-stress` (10 projects) and one project with 14 open tasks and parallel packages, tests listed in DELIVERY-SLICE.md criterion 10, captures to `build-evidence/COLONY-SLICE-1/runtime-greybox/`.

### Asset producer (lead branch, `producer/` only)

1. Read INPUT-CONTRACT "Asset producer checklist" and HQ-FLOOR-PLAN; read `hq_lib.py` and keep or discard it.
2. Build `hq-shell.glb` (groups `MF_BaseFixed`, `MF_ShellCutaway` + `_FrontFlats`/`_PartitionGlass`, `MF_Interior` + one child per room, `MF_Court`, `MF_Props`, `MF_Practicals`, `MF_Sockets` empties), `crown-command.glb` (`MF_Roof`, identity materials neutral), `bridge-span-27.glb`, `bridge-end.glb`, `hq-metadata.json`, validator, previews, `HANDOFF.md`. Relay and Foundry crowns only after Command validates.
3. Budgets: shell ≤150k tris / 15 MB, crown ≤40k / 5 MB, span ≤15k / 2 MB, ≤8 textures ≤2048², embedded PBR only.

### Terrain builder (lead branch, `terrain/` only)

1. Read TERRAIN-HOLES.md, INPUT-CONTRACT "Terrain builder checklist", `contract.colony.landRules` and `colony.edges`.
2. Build `parcel-hub.glb` and `parcel-a.glb` (closed manifold core, outward normals, single-sided terrain materials; plateau 4.0 to r 34, ring road 27.5–32.5, six spurs and pads on the edge world angles, abutment faces at r 40.5, coast ≤ r 46, seabed −3.0, backdrop only 200–340°), `parcel-metadata.json` with shoreline loops ≤120 points, validator with boundary-edge, winding and ray-cast checks, front-side-only previews, `HANDOFF.md`.
3. Budgets: ≤120k tris / 12 MB per parcel.

### Lead integration (after all three)

1. Merge the runtime branch into the lead branch (or the reverse), run `node scripts/frontier/integrate-3d-proof.mjs`, then `npm run -s test:frontier`, `typecheck`, `lint`, `format:check`, `build:frontier`.
2. Browser acceptance against the ten criteria in DELIVERY-SLICE.md, evidence into `build-evidence/COLONY-SLICE-1/`, journal entry, then open a PR to `main`.

## Commands

```sh
cd /Users/shaun/projects/agent-harness-ui/.claude/worktrees/agent-aa898a862c65647f8 && git merge --no-edit main && npm run -s test:frontier
```

```sh
npm run dev:frontier -- --host 127.0.0.1 --port 5241 --strictPort
```

URL: `http://127.0.0.1:5241/?mode=fixture&scenario=workflow&art=cinematic&renderer=3d#world`

```sh
/Applications/Blender.app/Contents/MacOS/Blender -b -t 4 --python <script.py> -- <args>
```

## Rules carried over

- Producer writes only under `producer/`, terrain only under `terrain/`, runtime only in its own worktree. The lead commits producer and terrain work.
- Never bare `git stash` / `git stash pop` (shared stash stack); use a WIP commit.
- Nothing runtime-owned is baked into assets: project identity, task status, actors, fixture ids. `identity_*` materials stay neutral in source.
- Do not re-ask the four decisions above.
